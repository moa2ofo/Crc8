name: CI

on:
  push:
    branches: ["**"]
  pull_request:

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install clang-format
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-format

      - name: Run clang-format on all .c and .h files
        run: |
          find . -type f \( -name "*.c" -o -name "*.h" \) \
            -not -path "./build/*" \
            -not -path "./.git/*" \
            -exec clang-format -i {} \;

      - name: Commit and push changes (if any)
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"

          if ! git diff --quiet; then
            git add .
            git commit -m "Auto-format: clang-format applied"
            git push
          else
            echo "No formatting changes needed."
          fi

  unit_tests:
    runs-on: ubuntu-latest

    steps:
      # 1) Checkout *your* repo (where this workflow lives)
      - name: Checkout current repository
        uses: actions/checkout@v4

      # 2) Checkout external repo into ./unitTestLauncher
      - name: Checkout unitTestLauncher repo
        uses: actions/checkout@v4
        with:
          repository: moa2ofo/unitTestLauncher
          path: unitTestLauncher
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Verify Docker is available
        run: docker info

      # 3) Run the launcher from the downloaded repo
      - name: Run unit tests (launcher repo)
        run: |
          # 'script' provides a pseudo-TTY so `docker run -it` works in CI
          script -q -e -c "python3 unitTestLauncher/unitTestsLauncher.py all" /dev/null

      # Optional: upload results if the launcher writes them in your main repo workspace
      - name: Upload unit test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-results
          path: |
            utExecutionAndResults/utResults
            unitTestLauncher/utExecutionAndResults/utResults
